# Ikrand Generator

## Requirements
* node `^4.5.0`
* npm `^3.0.0`

## Installation & Starting

```bash
$ npm install                   # Install project dependencies
$ npm start                     # Compile and launch
```
